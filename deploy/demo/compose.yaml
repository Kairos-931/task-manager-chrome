services:
  taskmaster-demo:
    image: taskmaster-demo:3.15.1
    build:
      context: ../..
      dockerfile: deploy/demo/Dockerfile
    restart: unless-stopped
    ports:
      - "${DEMO_PORT:-8080}:80"
